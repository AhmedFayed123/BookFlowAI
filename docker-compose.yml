services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: bookflow_sqlserver
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=YourStrongPassword123!
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          '/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "$$MSSQL_SA_PASSWORD" -C -Q "SELECT 1" -b || /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P "$$MSSQL_SA_PASSWORD" -C -Q "SELECT 1" -b',
        ]
      interval: 20s
      timeout: 15s
      retries: 12
      start_period: 40s
    networks:
      - bookflow-network

  ai_service:
    build:
      context: ./ai_service
      dockerfile: Dockerfile
    container_name: bookflow_ai_service
    ports:
      - "8000:8000"
    volumes:
      - ./ai_service:/app
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/readyz').read() or urllib.request.urlopen('http://localhost:8000/healthz').read()",
        ]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
    networks:
      - bookflow-network

  webapi:
    image: bookflow-api
    container_name: bookflow_api
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "5000:8080"
    volumes:
      - api_data_protection:/root/.aspnet/DataProtection-Keys
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=BookFlowDb;User Id=sa;Password=YourStrongPassword123!;TrustServerCertificate=True;MultipleActiveResultSets=true
      - AiService__BaseUrl=http://ai_service:8000
      - JwtSettings__Secret=${JWT_SECRET:-super-secret-key-please-change-this-now-12345}
      - JwtSettings__Issuer=BookFlowAI
      - JwtSettings__Audience=BookFlowAI-Clients
    depends_on:
      sqlserver:
        condition: service_healthy
      ai_service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/8080'"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 30s
    networks:
      - bookflow-network

  frontend:
    image: node:20-alpine
    container_name: bookflow_frontend
    working_dir: /app
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    command: sh -c "npm ci --no-audit --no-fund && npm run dev"
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:5000/api
      - NEXT_PUBLIC_SIGNALR_URL=http://localhost:5000/hubs/bookings
      - NEXT_PUBLIC_AI_API_URL=http://localhost:5000/api
    depends_on:
      webapi:
        condition: service_healthy
    networks:
      - bookflow-network

volumes:
  sqlserver_data:
  frontend_node_modules:
  api_data_protection:

networks:
  bookflow-network:
    driver: bridge
